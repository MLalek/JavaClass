package day33exceptionsdt;

public class E05 {
	
	/*
	 * What is the difference between "throw" and "throws" keywords?
	 * 
	 * 1) "throw" is used inside the method body, "throws" is used between method
	 * parentheses and the opening curly brace
	 * 
	 * 2) After "throw" we type ==> "new + constructor of Exception class"
	 * 		After "throws" we type ==> "name of Exception class"
	 * 
	 * 3) We can throw multiple exceptions by using just one "throws" keyword.
	 * 		We cannot throw multiple exceptions by using just one "throws" keyword.
	 * If you want to throw more than 1 exception by using "throw" keyword you have 
	 * to type "throw" per each.
	 * 
	 * 4) "throws" is used for the possible problems which Java knows. 
	 * 	"throw" keyword is used for the possible problems which we decide.
	 *  	
	 */

}
